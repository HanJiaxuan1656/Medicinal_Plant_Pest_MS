<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.medicinalplant.mapper.PestDiseaseMapper">
    
    <resultMap id="BaseResultMap" type="com.example.medicinalplant.entity.PestDisease">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="description" property="description"/>
        <result column="symptoms" property="symptoms"/>
        <result column="image_url" property="imageUrl"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="view_count" property="viewCount"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        id, name, type, description, symptoms, image_url, created_at, updated_at, created_by, view_count
    </sql>
    
    <select id="findAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        ORDER BY created_at DESC
    </select>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        WHERE id = #{id}
    </select>
    
    <select id="findByConditions" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        <where>
            <if test="search != null and search != ''">
                AND name LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    
    <select id="countByConditions" resultType="int">
        SELECT COUNT(*)
        FROM pest_disease
        <where>
            <if test="search != null and search != ''">
                AND name LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pest_disease (name, type, description, symptoms, image_url, created_at, updated_at, created_by)
        VALUES (#{name}, #{type}, #{description}, #{symptoms}, #{imageUrl}, #{createdAt}, #{updatedAt}, #{createdBy})
    </insert>
    
    <update id="update">
        UPDATE pest_disease
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="description != null">description = #{description},</if>
            <if test="symptoms != null">symptoms = #{symptoms},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            updated_at = #{updatedAt}
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM pest_disease WHERE id = #{id}
    </delete>

    <!-- 批量删除病虫害 -->
    <delete id="deleteByIds">
        DELETE FROM pest_disease WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findByType" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        WHERE type = #{type}
        ORDER BY created_at DESC
    </select>
    
    <select id="findByName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        WHERE name LIKE CONCAT('%', #{name}, '%')
        ORDER BY created_at DESC
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM pest_disease
    </select>
    
    <select id="countByType" resultType="int">
        SELECT COUNT(*) FROM pest_disease WHERE type = #{type}
    </select>
    
    <select id="findRecentPestDiseases" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pest_disease
        WHERE created_at >= DATE_SUB(CURRENT_TIMESTAMP, INTERVAL 7 DAY)
        ORDER BY created_at DESC
        LIMIT 5
    </select>

    <!-- 统计指定专家创建的病虫害数量 -->
    <select id="countByCreatedBy" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM pest_disease WHERE created_by = #{createdBy}
    </select>

    <!-- 增加查看次数 -->
    <update id="incrementViewCount">
        UPDATE pest_disease SET view_count = COALESCE(view_count, 0) + 1 WHERE id = #{id}
    </update>
</mapper>